{
  "https://ca.indeed.com/": {
    "search": {
      "script": "try {\n  const { keywords, location, jobType, minSalary, _verifyMode } = params;\n\n  // Check for anti-bot on initial page\n  await page.checkAntiBot({ maxWait: 10000 });\n\n  // Fill keywords\n  await page.waitForSelector('#text-input-what', { timeout: 8000 });\n  await page.randomDelay(800, 1500);\n  await page.humanType('#text-input-what', keywords || '');\n  await page.randomDelay(500, 1000);\n\n  // Fill location\n  if (location) {\n    await page.humanType('#text-input-where', location);\n    await page.randomDelay(500, 1000);\n  }\n\n  // Submit search\n  await page.humanClick('button[type=\"submit\"].yosegi-InlineWhatWhere-primaryButton');\n  await page.waitForNavigation({ timeout: 15000 });\n  await page.checkAntiBot({ maxWait: 10000 });\n  await page.randomDelay(1500, 2500);\n\n  // Apply job type filter if specified\n  if (jobType) {\n    const typeMap = {\n      'fulltime': 'fulltime',\n      'full-time': 'fulltime',\n      'parttime': 'parttime',\n      'part-time': 'parttime',\n      'contract': 'contract',\n      'temporary': 'temporary',\n      'internship': 'internship'\n    };\n    const jtKey = typeMap[(jobType || '').toLowerCase()] || jobType.toLowerCase();\n    const currentUrl = await page.url();\n    const sep = currentUrl.includes('?') ? '&' : '?';\n    await page.goto(currentUrl + sep + 'jt=' + jtKey);\n    await page.waitForNavigation({ timeout: 15000 }).catch(() => {});\n    await page.checkAntiBot({ maxWait: 10000 });\n    await page.randomDelay(1500, 2500);\n  }\n\n  // Apply salary filter if specified\n  if (minSalary) {\n    const currentUrl = await page.url();\n    const sep = currentUrl.includes('?') ? '&' : '?';\n    // Indeed uses salary param like salary=$60,000+ encoded\n    await page.goto(currentUrl + sep + 'salary=' + encodeURIComponent(minSalary));\n    await page.waitForNavigation({ timeout: 15000 }).catch(() => {});\n    await page.checkAntiBot({ maxWait: 10000 });\n    await page.randomDelay(1500, 2500);\n  }\n\n  // Wait for job cards to load\n  await page.waitForSelector('.job_seen_beacon, .jobsearch-ResultsList .result, .mosaic-provider-jobcards .result', { timeout: 10000 }).catch(() => {});\n  await page.randomDelay(1000, 2000);\n\n  // Phase 1: Extract job cards\n  const jobs = await page.evaluate(() => {\n    const results = [];\n    const cards = document.querySelectorAll('.job_seen_beacon, .mosaic-zone .result, div[class*=\"cardOutline\"]');\n\n    cards.forEach(card => {\n      try {\n        const titleEl = card.querySelector('.jcs-JobTitle, a[class*=\"JobTitle\"]');\n        const title = titleEl ? titleEl.textContent.trim() : '';\n        \n        let url = '';\n        if (titleEl) {\n          const link = titleEl.tagName === 'A' ? titleEl : titleEl.closest('a');\n          if (link) {\n            const rawHref = link.href || '';\n            // Prefer the real href; extract jk param for a clean canonical URL\n            if (rawHref) {\n              try {\n                const parsedUrl = new URL(rawHref, 'https://ca.indeed.com');\n                const jkParam = parsedUrl.searchParams.get('jk');\n                url = jkParam ? 'https://ca.indeed.com/viewjob?jk=' + jkParam : rawHref;\n              } catch(e) {\n                url = rawHref;\n              }\n            } else if (link.id) {\n              // Fallback: reconstruct from element id (sj_xxxx format)\n              const jk = link.id.replace(/^sj_/, '');\n              if (jk) url = 'https://ca.indeed.com/viewjob?jk=' + jk;\n            }\n          }\n        }\n\n        const companyEl = card.querySelector('[data-testid=\"company-name\"], .companyName, .company_location .companyName, span[class*=\"company\"]');\n        const company = companyEl ? companyEl.textContent.trim() : '';\n\n        const locationEl = card.querySelector('[data-testid=\"text-location\"], .companyLocation, div[class*=\"company_location\"] div[data-testid=\"text-location\"]');\n        const loc = locationEl ? locationEl.textContent.trim() : '';\n\n        const salaryEl = card.querySelector('.salary-snippet-container, div[class*=\"salary\"], .metadata.salary-snippet-container, [class*=\"SalarySnippet\"], div[class*=\"salaryOnly\"]');\n        const salary = salaryEl ? salaryEl.textContent.trim() : '';\n\n        const snippetEl = card.querySelector('.job-snippet, td.snip .job-snippet, [class*=\"JobSnippet\"], ul[style]');\n        const description = snippetEl ? snippetEl.textContent.trim() : '';\n\n        if (title) {\n          results.push({\n            title,\n            company,\n            url,\n            location: loc,\n            salary,\n            description,\n            fullText: ''\n          });\n        }\n      } catch (e) {\n        // skip bad card\n      }\n    });\n\n    return results;\n  });\n\n  // Phase 2: JD Detail Extraction\n  if (_verifyMode) {\n    jobs.forEach(j => { j.fullText = ''; });\n  } else {\n    for (let i = 0; i < jobs.length; i++) {\n      try {\n        // Click the job card title to open split-pane detail\n        const titleSelectors = '.jcs-JobTitle, a[class*=\"JobTitle\"]';\n        const allTitles = await page.$$(titleSelectors);\n        if (allTitles && allTitles.length > i) {\n          const sel = typeof allTitles[i] === 'string' ? allTitles[i] : titleSelectors + `:nth-of-type(${i + 1})`;\n          // Use evaluate to click the i-th title\n          await page.evaluate((idx) => {\n            const titles = document.querySelectorAll('.jcs-JobTitle, a[class*=\"JobTitle\"]');\n            if (titles[idx]) titles[idx].click();\n          }, i);\n          \n          await page.randomDelay(1500, 3000);\n\n          // Extract full job description from the detail panel\n          const fullText = await page.evaluate(() => {\n            const selectors = [\n              '#jobDescriptionText',\n              '.jobsearch-JobComponent-description',\n              '[class*=\"jobDescription\"]',\n              '.jobsearch-jobDescriptionText',\n              '#jobDescriptionText .jobsearch-JobComponent-description'\n            ];\n            for (const sel of selectors) {\n              const el = document.querySelector(sel);\n              if (el && el.textContent.trim().length > 50) {\n                return el.textContent.trim();\n              }\n            }\n            // Fallback: try the right pane\n            const rightPane = document.querySelector('.jobsearch-RightPane, [class*=\"RightPane\"], .jobsearch-ViewJobLayout--inline');\n            if (rightPane) return rightPane.textContent.trim();\n            return '';\n          });\n\n          jobs[i].fullText = fullText || '';\n        }\n      } catch (e) {\n        jobs[i].fullText = '';\n      }\n    }\n  }\n\n  return { success: true, jobs };\n\n} catch (error) {\n  return { success: false, error: error.message || String(error) };\n}",
      "version": 3,
      "savedAt": "2026-03-13T12:00:23.539Z",
      "jdVerified": true
    }
  },
  "https://www.linkedin.com/jobs": {
    "search": {
      "script": "try {\n  const maxResults = Math.max(1, Number(params.maxResults) || 30);\n  const pageOffset = Math.max(0, Number(params.pageOffset) || 0);\n  const keywords = (params.keywords || \"\").trim();\n  const location = params.location == null ? \"\" : String(params.location).trim();\n  const minSalary = params.minSalary != null && params.minSalary !== \"\" ? Number(params.minSalary) : null;\n\n  const normalizeUrl = (href, baseUrl) => {\n    try {\n      const u = new URL(href, baseUrl);\n      u.searchParams.delete(\"trackingId\");\n      u.searchParams.delete(\"refId\");\n      u.searchParams.delete(\"midToken\");\n      u.searchParams.delete(\"trk\");\n      return u.toString();\n    } catch {\n      return href || \"\";\n    }\n  };\n\n  const cssEscape = (value) =>\n    String(value).replace(/\\\\/g, \"\\\\\\\\\").replace(/\"/g, '\\\\\"');\n\n  const mapJobType = (value) => {\n    if (!value) return \"\";\n    const raw = String(value).trim().toLowerCase();\n    const map = {\n      \"full-time\": \"F\",\n      fulltime: \"F\",\n      full: \"F\",\n      \"part-time\": \"P\",\n      parttime: \"P\",\n      part: \"P\",\n      contract: \"C\",\n      temporary: \"T\",\n      temp: \"T\",\n      internship: \"I\",\n      intern: \"I\",\n      volunteer: \"V\",\n      other: \"O\"\n    };\n    return map[raw] || \"\";\n  };\n\n  const parseSalaryNumber = (text) => {\n    if (!text) return null;\n    const matches = String(text).match(/(?:CA?\\$|US?\\$|£|€)?\\s?(\\d{2,3}(?:,\\d{3})+|\\d{2,3})/g);\n    if (!matches || !matches.length) return null;\n    const values = matches\n      .map((m) => {\n        const n = m.replace(/[^\\d]/g, \"\");\n        return n ? Number(n) : null;\n      })\n      .filter((n) => Number.isFinite(n));\n    return values.length ? Math.max(...values) : null;\n  };\n\n  const waitForResultsOrEmpty = async (timeoutMs) => {\n    const started = Date.now();\n    while (Date.now() - started < timeoutMs) {\n      const state = await page.evaluate(() => {\n        const jobAnchors = Array.from(document.querySelectorAll('a[href*=\"/jobs/view/\"]'))\n          .filter((a) => {\n            const href = a.getAttribute(\"href\") || \"\";\n            const text = (a.textContent || \"\").trim();\n            return href.includes(\"/jobs/view/\") && text.length > 0 && a.offsetParent !== null;\n          });\n\n        const bodyText = (document.body?.innerText || \"\").toLowerCase();\n        const noResults =\n          /no results|0 results|no matching jobs|no jobs found|try different keywords/.test(bodyText) ||\n          !!document.querySelector('[data-test-empty-state], .jobs-search-no-results, .artdeco-empty-state');\n\n        return {\n          hasResults: jobAnchors.length > 0,\n          noResults\n        };\n      });\n\n      if (state.hasResults || state.noResults) return state;\n      await page.waitForTimeout(700);\n    }\n    return { hasResults: false, noResults: false };\n  };\n\n  const clearOrRetypeLocationIfNeeded = async () => {\n    const locationSelectors = [\n      'input[aria-label*=\"City\" i]',\n      'input[aria-label*=\"location\" i]',\n      'input[placeholder*=\"City\" i]',\n      'input[placeholder*=\"location\" i]'\n    ];\n\n    for (const selector of locationSelectors) {\n      if (await page.$(selector)) {\n        await page.waitForSelector(selector, { timeout: 7000, visible: true });\n        await page.humanType(selector, location || \"\");\n        await page.randomDelay(800, 1400);\n        await page.keyboard.press(\"Enter\");\n        await page.randomDelay(1500, 3000);\n        return true;\n      }\n    }\n\n    const clearSelectors = [\n      'button[aria-label*=\"Clear\" i]',\n      'a[aria-label*=\"Clear\" i]',\n      'button[title*=\"Clear\" i]'\n    ];\n\n    for (const selector of clearSelectors) {\n      if (await page.$(selector)) {\n        await page.waitForSelector(selector, { timeout: 5000, visible: true });\n        await page.humanClick(selector);\n        await page.randomDelay(1500, 3000);\n        return true;\n      }\n    }\n\n    return false;\n  };\n\n  const resultsPerPage = 25;\n  const start = pageOffset * resultsPerPage;\n  const url = new URL(\"https://www.linkedin.com/jobs/search-results/\");\n  url.searchParams.set(\"keywords\", keywords);\n  url.searchParams.set(\"location\", location);\n  url.searchParams.set(\"start\", String(start));\n\n  const jobTypeCode = mapJobType(params.jobType);\n  if (jobTypeCode) url.searchParams.set(\"f_JT\", jobTypeCode);\n\n  await page.goto(url.toString());\n  await page.checkAntiBot({ maxWait: 10000 });\n  await page.randomDelay(1000, 1800);\n\n  await page.waitForSelector(\"body\", { timeout: 8000, visible: true });\n\n  let state = await waitForResultsOrEmpty(12000);\n\n  if (state.noResults || !state.hasResults) {\n    await page.screenshot();\n    const recovered = await clearOrRetypeLocationIfNeeded();\n    if (recovered) {\n      state = await waitForResultsOrEmpty(12000);\n    }\n    if (state.noResults || !state.hasResults) {\n      return { success: true, jobs: [] };\n    }\n  }\n\n  const cardSelector = 'a[href*=\"/jobs/view/\"]';\n\n  let previousCount = 0;\n  for (let i = 0; i < 5; i++) {\n    const currentCount = (await page.$$(cardSelector)).length;\n    if (currentCount >= maxResults) break;\n\n    await page.evaluate(() => {\n      const selectors = [\n        \".jobs-search-results-list\",\n        \".scaffold-layout__list\",\n        '[data-results-list-container]',\n        \"main\"\n      ];\n\n      let container = null;\n      for (const selector of selectors) {\n        const el = document.querySelector(selector);\n        if (el && el.scrollHeight > el.clientHeight) {\n          container = el;\n          break;\n        }\n      }\n\n      if (!container) {\n        const candidates = Array.from(document.querySelectorAll(\"div, section, main\"))\n          .filter((el) => el.scrollHeight > el.clientHeight + 100)\n          .sort((a, b) => b.scrollHeight - a.scrollHeight);\n        container = candidates[0] || document.scrollingElement || document.body;\n      }\n\n      const nextTop = Math.min(\n        container.scrollTop + Math.max(600, Math.floor(container.clientHeight * 0.9)),\n        container.scrollHeight\n      );\n      container.scrollTo({ top: nextTop, behavior: \"smooth\" });\n    });\n\n    await page.randomDelay(1500, 2500);\n\n    const newCount = (await page.$$(cardSelector)).length;\n    if (newCount <= previousCount || newCount === currentCount) break;\n    previousCount = newCount;\n  }\n\n  const jobs = await page.$$eval(\n    cardSelector,\n    (anchors, limit, baseUrl) => {\n      const normalize = (href) => {\n        try {\n          const u = new URL(href, baseUrl);\n          u.searchParams.delete(\"trackingId\");\n          u.searchParams.delete(\"refId\");\n          u.searchParams.delete(\"midToken\");\n          u.searchParams.delete(\"trk\");\n          return u.toString();\n        } catch {\n          return href || \"\";\n        }\n      };\n\n      const out = [];\n      const seen = new Set();\n\n      for (const a of anchors) {\n        if (out.length >= limit) break;\n        if (!(a instanceof HTMLAnchorElement)) continue;\n        if (a.offsetParent === null) continue;\n\n        const rawHref = a.href || a.getAttribute(\"href\") || \"\";\n        if (!rawHref || !rawHref.includes(\"/jobs/view/\")) continue;\n\n        const url = normalize(rawHref);\n        if (!url || seen.has(url)) continue;\n        seen.add(url);\n\n        const container =\n          a.closest(\"li\") ||\n          a.closest(\"article\") ||\n          a.closest('[class*=\"job\"]') ||\n          a.closest(\"div\");\n\n        const text = (container?.innerText || a.innerText || \"\").replace(/\\u00a0/g, \" \").trim();\n        const lines = text\n          .split(\"\\n\")\n          .map((s) => s.trim())\n          .filter(Boolean);\n\n        const title = (a.textContent || lines[0] || \"\").trim();\n        let company = \"\";\n        let location = \"\";\n        let salary = \"\";\n        let description = \"\";\n\n        const moneyLine = lines.find((line) => /(?:CA?\\$|US?\\$|£|€|\\bper year\\b|\\byr\\b|\\bhour\\b)/i.test(line));\n        if (moneyLine) salary = moneyLine;\n\n        for (const line of lines) {\n          if (!company && line !== title && !/(promoted|easy apply|actively reviewing|be an early applicant)/i.test(line)) {\n            company = line;\n            continue;\n          }\n          if (!location && line !== title && line !== company && /[,]|remote|hybrid|on-site|onsite/i.test(line)) {\n            location = line;\n            continue;\n          }\n        }\n\n        description = lines\n          .filter((line) => ![title, company, location, salary].includes(line))\n          .slice(0, 3)\n          .join(\" \")\n          .trim();\n\n        const jobKey = String(out.length);\n        a.setAttribute(\"data-job-key\", jobKey);\n\n        out.push({\n          jobKey,\n          title,\n          company,\n          url,\n          location,\n          salary,\n          description,\n          fullText: \"\"\n        });\n      }\n\n      return out;\n    },\n    maxResults,\n    \"https://www.linkedin.com\"\n  );\n\n  const filteredJobs = minSalary\n    ? jobs.filter((job) => {\n        const value = parseSalaryNumber(job.salary);\n        return value == null || value >= minSalary;\n      })\n    : jobs;\n\n  if (params._verifyMode) {\n    filteredJobs.forEach((job) => {\n      job.fullText = \"\";\n      delete job.jobKey;\n    });\n    return { success: true, jobs: filteredJobs };\n  }\n\n  for (const job of filteredJobs) {\n    try {\n      const selector = `a[data-job-key=\"${cssEscape(job.jobKey)}\"]`;\n\n      if (await page.$(selector)) {\n        await page.waitForSelector(selector, { timeout: 7000, visible: true });\n        await page.humanClick(selector);\n        await page.randomDelay(1500, 3000);\n\n        const fullText = await page.evaluate(() => {\n          const isVisible = (el) => {\n            const rect = el.getBoundingClientRect();\n            const style = window.getComputedStyle(el);\n            return (\n              rect.width > 200 &&\n              rect.height > 120 &&\n              style.visibility !== \"hidden\" &&\n              style.display !== \"none\"\n            );\n          };\n\n          const candidates = Array.from(document.querySelectorAll(\"main, section, article, div\"))\n            .filter((el) => {\n              if (!isVisible(el)) return false;\n              const rect = el.getBoundingClientRect();\n              const text = (el.innerText || \"\").trim();\n              return rect.x > window.innerWidth * 0.25 && text.length > 300;\n            })\n            .map((el) => ({\n              text: (el.innerText || \"\").replace(/\\n{3,}/g, \"\\n\\n\").trim(),\n              score: ((el.innerText || \"\").trim().length) + Math.max(0, el.getBoundingClientRect().x)\n            }))\n            .sort((a, b) => b.score - a.score);\n\n          return candidates[0]?.text || \"\";\n        });\n\n        job.fullText = fullText || \"\";\n        if (!job.description && job.fullText) {\n          job.description = job.fullText.slice(0, 280).trim();\n        }\n        if (!job.salary && job.fullText) {\n          const salaryLine = job.fullText\n            .split(\"\\n\")\n            .map((s) => s.trim())\n            .find((line) => /(?:CA?\\$|US?\\$|£|€|\\bper year\\b|\\byr\\b|\\bhour\\b)/i.test(line));\n          if (salaryLine) job.salary = salaryLine;\n        }\n      } else {\n        job.fullText = \"\";\n      }\n    } catch {\n      job.fullText = \"\";\n    }\n\n    delete job.jobKey;\n  }\n\n  return { success: true, jobs: filteredJobs };\n} catch (error) {\n  return {\n    success: false,\n    error: error && error.message ? error.message : String(error)\n  };\n}",
      "version": 6,
      "jdVerified": true,
      "savedAt": "2026-03-15T19:08:29.454Z"
    }
  },
  "https://ca.indeed.com/jobs": {
    "search": {
      "script": "try {\n  const baseUrl = 'https://ca.indeed.com';\n  const maxResults = Math.max(1, Math.min(Number(params.maxResults) || 30, 100));\n  const pageOffset = Math.max(0, Number(params.pageOffset) || 0);\n  const resultsPerPage = 10;\n\n  const normalizeJobType = (jobType) => {\n    const value = String(jobType || '').trim().toLowerCase();\n    const map = {\n      'full-time': 'fulltime',\n      'full time': 'fulltime',\n      fulltime: 'fulltime',\n      'part-time': 'parttime',\n      'part time': 'parttime',\n      parttime: 'parttime',\n      contract: 'contract',\n      temporary: 'temporary',\n      internship: 'internship',\n      intern: 'internship',\n      casual: 'casual',\n      permanent: 'permanent',\n      freelance: 'subcontract'\n    };\n    return map[value] || '';\n  };\n\n  const buildSearchUrl = () => {\n    const url = new URL('/jobs', baseUrl);\n    url.searchParams.set('q', params.keywords || '');\n    url.searchParams.set('l', params.location || '');\n    url.searchParams.set('start', String(pageOffset * resultsPerPage));\n    url.searchParams.set('from', 'searchOnHP');\n\n    const jt = normalizeJobType(params.jobType);\n    if (jt) url.searchParams.set('jt', jt);\n\n    const minSalary = Number(params.minSalary);\n    if (!Number.isNaN(minSalary) && minSalary > 0) {\n      url.searchParams.set('salary', String(Math.round(minSalary)));\n    }\n\n    return url.toString();\n  };\n\n  const waitForResultsOrNoResults = async (timeoutMs) => {\n    const start = Date.now();\n    while (Date.now() - start < timeoutMs) {\n      const state = await page.evaluate(() => {\n        const cardSelectors = [\n          'a.jcs-JobTitle[href]',\n          '[data-testid=\"job-title\"] a[href]',\n          'a[href*=\"/viewjob\"]',\n          'a[href*=\"/pagead/clk\"]'\n        ];\n        const cards = cardSelectors.reduce((count, selector) => {\n          return count + document.querySelectorAll(selector).length;\n        }, 0);\n\n        const bodyText = (document.body && document.body.innerText ? document.body.innerText : '').toLowerCase();\n        const noResultsPhrases = [\n          'no results',\n          '0 results',\n          'no matching jobs',\n          'did not match any jobs',\n          'jobs not found',\n          'be the first to see new'\n        ];\n        const hasNoResultsText = noResultsPhrases.some((phrase) => bodyText.includes(phrase));\n\n        const emptyState =\n          !!document.querySelector('[data-testid=\"no-results-message\"]') ||\n          !!document.querySelector('[data-testid=\"searchNoResult\"]') ||\n          !!document.querySelector('.jobsearch-NoResult') ||\n          !!document.querySelector('#searchNoResultsContainer');\n\n        const whereInput = document.querySelector('#text-input-where, input[name=\"l\"]');\n        const displayedLocation = whereInput ? (whereInput.value || '').trim() : '';\n\n        return {\n          hasResults: cards > 0,\n          hasNoResults: hasNoResultsText || emptyState,\n          cardCount: cards,\n          displayedLocation\n        };\n      });\n\n      if (state.hasResults || state.hasNoResults) {\n        return state;\n      }\n\n      await page.waitForTimeout(500);\n    }\n\n    return { hasResults: false, hasNoResults: false, cardCount: 0, displayedLocation: '' };\n  };\n\n  const tryRecoverLocation = async () => {\n    const locationSelector = '#text-input-where';\n    const hasLocationInput = await page.$(locationSelector);\n    if (!hasLocationInput) return false;\n\n    await page.waitForSelector(locationSelector, { timeout: 7000, visible: true });\n    await page.humanType(locationSelector, params.location || '');\n    await page.randomDelay(800, 1600);\n    await page.keyboard.press('Enter');\n    await page.randomDelay(1200, 2200);\n    await page.checkAntiBot({ maxWait: 8000 });\n    return true;\n  };\n\n  const scrollResults = async () => {\n    let previousCount = 0;\n\n    for (let i = 0; i < 5; i++) {\n      const currentCount = await page.$$eval(\n        'a.jcs-JobTitle[href], [data-testid=\"job-title\"] a[href], a[href*=\"/viewjob\"], a[href*=\"/pagead/clk\"]',\n        (nodes) => nodes.length\n      ).catch(() => 0);\n\n      if (currentCount >= maxResults) break;\n\n      await page.evaluate(() => {\n        const container =\n          document.querySelector('#mosaic-jobResults') ||\n          document.querySelector('[data-testid=\"search-results\"]') ||\n          document.querySelector('#jobsearch-ResultsList') ||\n          document.querySelector('main') ||\n          document.scrollingElement ||\n          document.body;\n\n        if (container && container !== document.body && container !== document.documentElement) {\n          container.scrollBy(0, Math.max(800, container.clientHeight || 900));\n        } else {\n          window.scrollBy(0, Math.max(window.innerHeight * 0.9, 900));\n        }\n      });\n\n      await page.waitForTimeout(1800 + i * 150);\n      const newCount = await page.$$eval(\n        'a.jcs-JobTitle[href], [data-testid=\"job-title\"] a[href], a[href*=\"/viewjob\"], a[href*=\"/pagead/clk\"]',\n        (nodes) => nodes.length\n      ).catch(() => 0);\n\n      if (newCount <= previousCount || newCount === currentCount) {\n        break;\n      }\n      previousCount = newCount;\n    }\n  };\n\n  const searchUrl = buildSearchUrl();\n  await page.goto(searchUrl);\n  await page.waitForSelector('body', { timeout: 8000, visible: true });\n  await page.randomDelay(800, 2000);\n  await page.checkAntiBot({ maxWait: 10000 });\n\n  let state = await waitForResultsOrNoResults(12000);\n\n  if (state.hasNoResults && !state.hasResults) {\n    await page.screenshot();\n\n    const expectedLocation = String(params.location || '').trim().toLowerCase();\n    const shownLocation = String(state.displayedLocation || '').trim().toLowerCase();\n    const locationMismatch = expectedLocation !== shownLocation;\n\n    if (locationMismatch || expectedLocation === '' || shownLocation) {\n      const recovered = await tryRecoverLocation();\n      if (recovered) {\n        state = await waitForResultsOrNoResults(12000);\n      }\n    }\n\n    if (state.hasNoResults && !state.hasResults) {\n      return { success: true, jobs: [] };\n    }\n  }\n\n  if (!state.hasResults) {\n    return { success: true, jobs: [] };\n  }\n\n  await scrollResults();\n\n  const jobs = await page.$$eval(\n    'a.jcs-JobTitle[href], [data-testid=\"job-title\"] a[href], a[href*=\"/viewjob\"], a[href*=\"/pagead/clk\"]',\n    (links, base, limit) => {\n      const seen = new Set();\n      const items = [];\n\n      for (const link of links) {\n        if (items.length >= limit) break;\n\n        const href = link.getAttribute('href') || '';\n        const resolvedUrl = (() => {\n          try {\n            return new URL(href, base).toString();\n          } catch {\n            return href || '';\n          }\n        })();\n\n        const dedupeKey = resolvedUrl || href || link.textContent || '';\n        if (!dedupeKey || seen.has(dedupeKey)) continue;\n        seen.add(dedupeKey);\n\n        const card =\n          link.closest('[data-jk]') ||\n          link.closest('li') ||\n          link.closest('article') ||\n          link.closest('.cardOutline') ||\n          link.closest('.slider_item') ||\n          link.closest('div');\n\n        const text = (el) => (el && el.textContent ? el.textContent.replace(/\\s+/g, ' ').trim() : '');\n\n        const queryText = (selectors) => {\n          for (const selector of selectors) {\n            const node = card ? card.querySelector(selector) : null;\n            const value = text(node);\n            if (value) return value;\n          }\n          return '';\n        };\n\n        const title = text(link);\n        const company = queryText([\n          '[data-testid=\"company-name\"]',\n          '.companyName',\n          '[data-testid=\"company-title\"]',\n          'span[data-testid=\"company-name\"]'\n        ]);\n\n        const location = queryText([\n          '[data-testid=\"text-location\"]',\n          '.companyLocation',\n          '[data-testid=\"job-location\"]'\n        ]);\n\n        const salary = queryText([\n          '[data-testid=\"attribute_snippet_testid\"]',\n          '.salary-snippet-container',\n          '.estimated-salary-container',\n          '.salaryOnly'\n        ]);\n\n        const description = queryText([\n          '[data-testid=\"job-snippet\"]',\n          '.job-snippet',\n          '.underShelfFooter'\n        ]);\n\n        items.push({\n          title,\n          company,\n          url: resolvedUrl || href,\n          location,\n          salary,\n          description,\n          fullText: ''\n        });\n      }\n\n      return items;\n    },\n    baseUrl,\n    maxResults\n  );\n\n  if (params._verifyMode) {\n    jobs.forEach((job) => {\n      job.fullText = '';\n    });\n    return { success: true, jobs };\n  }\n\n  await page.evaluate(() => {\n    const links = Array.from(\n      document.querySelectorAll('a.jcs-JobTitle[href], [data-testid=\"job-title\"] a[href], a[href*=\"/viewjob\"], a[href*=\"/pagead/clk\"]')\n    );\n    links.forEach((link, index) => {\n      link.setAttribute('data-search-index', String(index));\n    });\n  });\n\n  for (let i = 0; i < jobs.length; i++) {\n    try {\n      const clicked = await page.evaluate((jobUrl) => {\n        const normalize = (value) => {\n          try {\n            return new URL(value, location.origin).toString();\n          } catch {\n            return value || '';\n          }\n        };\n\n        const links = Array.from(\n          document.querySelectorAll('a.jcs-JobTitle[href], [data-testid=\"job-title\"] a[href], a[href*=\"/viewjob\"], a[href*=\"/pagead/clk\"]')\n        );\n\n        const idx = links.findIndex((link) => normalize(link.href) === normalize(jobUrl));\n        if (idx >= 0) {\n          links[idx].setAttribute('data-target-job', 'true');\n          return true;\n        }\n        return false;\n      }, jobs[i].url);\n\n      const targetSelector = clicked\n        ? 'a[data-target-job=\"true\"]'\n        : `a.jcs-JobTitle[data-search-index=\"${i}\"], [data-testid=\"job-title\"] a[data-search-index=\"${i}\"]`;\n\n      await page.waitForSelector(targetSelector, { timeout: 7000, visible: true });\n      await page.humanClick(targetSelector);\n      await page.randomDelay(1500, 3000);\n      await page.checkAntiBot({ maxWait: 6000 });\n\n      const fullText = await page.evaluate(() => {\n        const candidates = [\n          document.querySelector('#jobDescriptionText'),\n          document.querySelector('[data-testid=\"jobsearch-JobComponent-description\"]'),\n          document.querySelector('.jobsearch-JobComponent-description'),\n          document.querySelector('[data-testid=\"job-details\"]'),\n          document.querySelector('#jobsearch-ViewjobPaneWrapper')\n        ].filter(Boolean);\n\n        let bestText = '';\n        for (const root of candidates) {\n          const directText = root && root.innerText ? root.innerText.replace(/\\s+\\n/g, '\\n').replace(/\\n{3,}/g, '\\n\\n').trim() : '';\n          if (directText.length > bestText.length) bestText = directText;\n\n          const descendants = root ? Array.from(root.querySelectorAll('div, section, article')) : [];\n          for (const node of descendants) {\n            const t = node && node.innerText ? node.innerText.replace(/\\s+\\n/g, '\\n').replace(/\\n{3,}/g, '\\n\\n').trim() : '';\n            if (t.length > bestText.length) bestText = t;\n          }\n        }\n\n        return bestText;\n      });\n\n      jobs[i].fullText = fullText || '';\n      await page.evaluate(() => {\n        const marked = document.querySelector('a[data-target-job=\"true\"]');\n        if (marked) marked.removeAttribute('data-target-job');\n      });\n    } catch {\n      jobs[i].fullText = '';\n      await page.evaluate(() => {\n        const marked = document.querySelector('a[data-target-job=\"true\"]');\n        if (marked) marked.removeAttribute('data-target-job');\n      }).catch(() => {});\n    }\n  }\n\n  return { success: true, jobs };\n} catch (error) {\n  return {\n    success: false,\n    error: error && error.message ? error.message : String(error)\n  };\n}",
      "version": 7,
      "jdVerified": true,
      "savedAt": "2026-03-15T18:31:58.727Z"
    }
  }
}